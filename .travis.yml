dist: trusty

language: go
go:
  - "1.12.x"

git:
  depth: 3

install: make

branches:
  only:
  - twentyfourbytes

deploy:
  provider: script
  script: bash bin/docker_build
  on:
    branch: twentyfourbytes

